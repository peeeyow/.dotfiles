return {
  {
    "ray-x/lsp_signature.nvim",
    opts = {
      debug = false,
      verbose = true,
      bind = true,
      doc_lines = 10,

      floating_window = true,

      floating_window_above_cur_line = true,
      noice = true,

      max_height = 12,
      hint_enable = true,
      hint_prefix = "",
      max_width = 50,
    },
    enabled = false,
  },
}
