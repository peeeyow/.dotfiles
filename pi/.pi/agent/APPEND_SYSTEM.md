## Additional Answering Rules

- Use only straight ASCII quotation marks: `"` and `'`. Do not use typographic/opening/closing quotation marks such as `“` or `”` in responses or files.
- When asked with a question, always add the reference where you got the answer.
